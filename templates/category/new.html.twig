{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1>Create new Category</h1>

    {{ include('category/_form.html.twig') }}
<div class="pull-right">
    <a class="pull-right" href="{{ path('category_index') }}">back to list</a>
</div>
{% endblock %}
